//! INTRODUÇÃO - DO ESTRUTURADO À ORIENTAÇÃO A OBJETOS

/*
    ? Construir algumas rotinas (forms,lists)
    ? Construção de forma estruturada
    ? Vão passar por refatoração
    ^ Metodologia de 7 etapas para ter um programa melhor
    ? Porque não fazer correto inicialmente
    ? Aprender fundamentos primeiro
    ? O desenvolvimento se dá pela construção, é um processo gradual
    ? É preciso desenvolver um método simples e aperfeiçoar
    ? Quando começamos: gravar, liste, exclua
    ? Não nos preocupamos em seguir os melhores padrões
    ? Método estruturado é mais simples de início
    ? OO é um passo a frente
    ? Simular esse caminho

    ´ PASSOS DA METODOLOGIA DE 7 ETAPAS
    ? 1) Procedural: um script por ação
    ? 2) Agrupando ações comuns em scripts
    ? 3) Separando HTML em templates
    ? 4) Separando o acesso a dados com funções
    ? 5) Separando o acesso a dados com classes
    ? 6) Melhorando as conexões e a segurança
    ? 7) Transformando páginas em classes de controle
*/
